const helpers = {
    helper1: function() {
        console.log('hi i am the helper function');
    },
    checkHorizontal: function (c1, c2, c3, c4, c5, c6, c7) {
        //need to check for horizontal winner starting from last index of each column
        let winner = false;
        let arr= [];
        for (let i = c1.length-1; i >=0; i--) {

        }
        return winner;
    }

}
export default helpers;